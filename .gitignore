# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environments
.venv/
venv/

# Generator output
events.jsonl
*.jsonl

# Eval comparison scratch
results/_compare_*/

# Local documentation (kept out of git for now, by request)
documentation/

# Claude planning artifacts (brainstorm specs + implementation plans) — local only
docs/superpowers/

# Internal working notes kept local. NOTE: the rest of docs/ IS tracked on
# purpose — it holds the images the README embeds and the scripts that
# regenerate them, so do not ignore docs/ wholesale.
docs/hardening-plan.md
docs/live-demo-storyboard.md

# Interview prep — local only
interview-prep/

# Project brief
BRIEF_for_Claude_Code.md

# Local working notes
tasks/

# Local secrets — API keys etc. NEVER commit these.
.env.local
.env

# Local Claude Code config (launch configs etc.)
.claude/
